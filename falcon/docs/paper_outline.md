# 論文の骨組み ― 主張を 1 本に絞る

現状、リポジトリには**主張が 4 本**混ざっている:
再現性・性能・定数時間・仕様書の穴。
1 本の論文にできるのは 1 本だけである。

---

## 選ぶ主張（2026-08-21、関連研究調査を受けて改訂）

> **FALCON には、仕様書が中間値を決定していない箇所が複数ある。
> 我々が測ったすべての場合において、その不決定性は署名まで届かない。**

### なぜこの形か

初版は「独立実装が参照実装の署名をビット単位で再現できる」を主結果に置いた。
関連研究調査（`docs/related_work_prompt.md` の回答）で、それが
**最も「既知の再発見」に終わる危険が高い**と判定された:

- 著者自身の C / Rust / Go 実装がすでに相互運用（ビット一致）を前提としている
  （`pornin/rust-fn-dsa` の README「All internal behaviour (in particular
  rounding) should be strictly identical on all platforms」）。
- **ePrint 2024/1709「Do Not Disturb a Sleeping Falcon」**（Lin, Tibouchi, Yu,
  Zhang, EUROCRYPT 2025）が、dynamic/tree モード差と FMA 有無による
  丸めの差が署名に**伝播し**、derandomized Falcon で鍵回復に至ることを示している。
  こちらの主張 2(c)（`ffSampling` 最下段の係数畳み込み）は
  同論文の split/merge 演算順序の解析と**直接重なる**。

したがって「再現できた」を主結果にすると、逆命題の焼き直しと読まれる。

**そこで軸を変える。** 同論文が示したのは「**あるクラスの**丸めの差は伝播する」で、
本稿が示すのは「**別のクラスの**不決定性は伝播しない」である。
両者を合わせると**伝播する差としない差の境界**が引ける。
これは片方だけでは書けない。

### 主結果を構成する 3 つの不決定性

| # | 箇所 | 仕様書が決めていないもの | 実装が違えば何が変わるか | 署名は変わるか |
|--:|:---|:---|:---|:---|
| A | 複素除算 / `LDL*` の `D11` / `ffSampling` 最下段 | **演算の書き方**（代数的には同値） | 展開秘密鍵の全 `L10` | **変わらない**（0/480 署名、0/245760 係数） |
| B | `fpr_inv_sigma[logn]` | **定数そのもの**（Table 3.3 の σ から導出できない） | 木の葉 444/512 | **変わらない**（0/120 署名、0/61440 係数） |
| C | `sigma_min` の ε | **導出**（値のみ印字。σ とは別の ε） | σ_min を再導出した実装の値 | ― （下限としてしか使われない） |

**A と B は独立に発見され、独立に測定され、同じ結論に達した。**
それが本稿の主張である。

### 関連研究に対する位置

| 先行研究 | 何を示したか | 本稿との関係 |
|:---|:---|:---|
| ePrint 2024/1709 | dyn/tree モード差・FMA が署名に**伝播**、derandomized で鍵回復 | **対** ― 本稿は別クラスの差が伝播しないことを示す。境界を引く |
| ePrint 2024/321（Hwang, IWSEC 2024） | エミュレート浮動小数点乗算の仕様との相違を形式検証で同定し、**複素 FFT に影響しない**ことを検証 | **最も方法論が近い**。対象が違う（乗算のゼロ化 vs 除算・LDL・定数）。明示的に引くこと |
| ePrint 2024/1769（A Closer Look at Falcon） | σ の選定と GPV 証明のギャップ、Falcon+ | σ_min の ε には触れていない。C の補強 |
| ePrint 2019/1411（Isochronous Gaussian Sampling） | 現行サンプラの等時間性設計 | 付録の定数時間節が拠る |

### 捨てる／降格する

- **性能**。同着〜2 倍遅い。§「実装」で範囲比較として述べるだけ。
- **定数時間**。攻撃を示していないので付録。
  最大の漏洩は**自分の実装の欠陥**で参照実装には無かった（#052）。
- **Julia であること**。§1 の動機（`docs/why_julia.md`）だけ。

## 構成案

### 1. Introduction

- FALCON / FN-DSA は署名が**浮動小数点の関数**である唯一の NIST 署名標準。
  FIPS 204（ML-DSA）は "No Floating-Point Arithmetic" を明示的な節に持ち、
  同一入力・同一乱数なら同一出力が保証される。**FALCON にはその保証がない。**
- FIPS 206 は演算順序を規定し FMA を禁じ、署名が KAT にビット一致することを
  要求する方向である（Perlner, NIST, 2025-09）。
  **ならば「仕様書は中間値を決定しているか」は規範上の問いである。**
- 動機（正直に）: 我々は速度を期待して Julia を選んだ。
  期待は部分的にしか当たらなかった（`docs/why_julia.md`）が、
  **別言語で書いたことが仕様書と参照実装の差を可視化した。**
  同じ言語で書き直していたら「同じコードだから同じ」で終わっていた。
- 貢献:
  1. 仕様書が中間値を決定していない箇所を **3 クラス**同定する
     （演算の書き方・定数そのもの・導出の欠落）
  2. それぞれについて、**実装が違えば何が変わり、署名は変わらない**ことを
     厳密な bit 比較と大標本で測る
  3. ePrint 2024/1709 が示した「伝播する差」との**境界**を与える
  4. 参照実装から独立に書かれた実装が、参照実装の署名を
     ビット単位で再現することを示す（上記を測るための実験装置）

### 2. Background

- FALCON の署名生成、ffLDL 木、ffSampling、SamplerZ
- なぜ浮動小数点が本質か（FPEMU の存在理由）
- **関連研究**（`docs/related_work_prompt.md` の調査結果を反映）
  - 伝播する側: ePrint 2024/1709
  - 方法論の隣人: ePrint 2024/321（形式検証で相違を同定し非伝播を検証）
  - パラメータ側: ePrint 2024/1769
  - サンプラの等時間性: ePrint 2019/1411

### 3. Methodology

ここは**弱点ではなく強み**として書く。本リポジトリの記録は
計測器の失敗を 6 回含んでおり、そのすべてが対照によって検出された。

- 比較は**生の 64 bit パターン**で行う。「1e-12 で一致」は
  `samplerz` が中心を丸める以上、何も言っていない。
- 変更の効果は**変更を戻して**測る（#043、#044、#050）。
- 計測器には**陽性・陰性の対照**を常設し、雑音床を実測する（#051）。
- C は**ビルドの集合**として測る（#052）。
- Julia は**run 間の幅**を出す。

### 4. Bit-exact reproduction

- 実験装置: 参照実装を共有ライブラリにし、`prng` の 56 バイト状態を
  直接与える shim（`scripts/cref_shim.c`）。
  **これが本当の障害だった** ― 参照実装の公開 API は
  サンプラの ChaCha20 を内部で SHAKE から導出するので、
  乱数を固定する手段がなかった。
- 段階的な突き合わせ（`scripts/cmp_cref_fp.jl`）:
  - 無改修で bit 一致: `fft` `ifft` `split_fft` `merge_fft`
    `add` `sub` `mul` `adj` `mul*adj` `mul*selfadj`、`B0` 行列
  - 書き方を合わせて一致: 複素除算、`LDL*` の `D11`、
    `ffSampling` の最下 2 段
  - 規約が違う: 木の葉（互いの逆数）、`fpr_inv_sigma`
- 結果: 展開秘密鍵 5120/5120、`s2` 25/25、圧縮バイト 25/25
- **反証**: 3 箇所を戻しても再現する。480 署名・245760 係数で差 0。
  → **「浮動小数点の書き方の差」は署名に伝播しない。**
  伝播しないのは `berexp` の判定余裕が 1 ulp より桁違いに大きいためで、
  1 draw あたりの反転確率は 2^-52 程度、1 署名で 2^-43 程度。

### 5. The constants: `fpr_inv_sigma` cannot be derived from the specification

`scripts/inv_sigma_audit.jl` が生データを出す。400 bit で計算し、
生のビットパターンで比較する。

参照実装は ffLDL 木の葉に `fpr_inv_sigma[logn]` を掛ける
（`ffLDL_binary_normalize`: "we actually store in the tree leaf the inverse of
the value mandated by the specification"）。この定数は:

| 導出 | logn=9 | logn=10 |
|:---|---:|---:|
| Table 3.3 の印字値（12 桁）の正しく丸めた逆数 | **+941 ulp** | **+18545 ulp** |
| Python 参照実装の Float64 σ の正しく丸めた逆数 | +1 ulp | +2 ulp |
| Float64 除算 `1.0/σ` | +1 ulp | +2 ulp |
| (2.13) を 400 bit で評価した σ の逆数 | +1 ulp | +1 ulp |
| (2.13) を Float64 で評価してから `1.0/` | +2 ulp | +1 ulp |

**構成できるどの導出とも一致せず、常に上方にずれる。**
さらに fpr.h の 35 桁十進リテラルは `1/σ` の 35 桁展開ではない ―
有効数字 16 桁ほどで分岐する。つまりこのリテラルは
高精度計算の出力ではなく、**ある Float64 を 35 桁で印字したもの**である。

**帰結**（ここを誇張しないこと）:

- Table 3.3 の σ から `1/σ` を計算する実装は、
  **展開秘密鍵が違う**（木の葉 512 個中 **444 個**が食い違う）。
- **署名は変わらない。** 同一鍵・同一メッセージ・同一 PRNG 状態で
  120 署名・61440 係数のうち **0 件**。
- したがって **署名 KAT には効かず、中間値 KAT・展開鍵形式には効く。**

**付随発見**: `inner.h:749` は `fpr_sigma_min[] 1/sigma_min` とコメントするが、
配列が保持するのは **σ_min そのもの**（1.2778…）である。
参照実装のヘッダコメント自体の誤り。

### 5b. `sigma_min` の ε は印字されていない

σ は (2.13) を `ε ≤ 1/√(Q_s·λ)`（`Q_s = 2^64`、`λ = 128/256`）で評価した値
（相対誤差 2e-16 で再現）。σ_min は Table 3.3 に**値しかない**。
Z の平滑化パラメータ

    η_ε(Z) = (1/π)·√( (1/2)·ln(2(1 + 1/ε)) )

を `ε = 1/√(2^64·n³)` で評価すると 3e-13 で再現する。
**(2.13) の ε とは別物**で、取り違えると 10〜11% ずれる。

> **注意**: (2.13) の対数の引数は `4n(1 + 1/ε)`（`Z^{2n}` の平滑化）、
> 上の η は `2(1 + 1/ε)`（`Z` の平滑化）。**別の式である。**
> 関連研究調査の回答はこの 2 つを混同して「(2.13) の記述が誤り」と
> 指摘してきたが、`test/test_spec.jl` が両方を数値で検証しており、
> 本稿の記述は正しい。査読でも同じ混同が起きうるので本文で明示すること。

### 5c. SamplerZ テストベクタの replay 規約

Table 3.2 の 16 本は、9 バイトを 72 bit 語に組む順序が本文に無い。
左から読むと 16 本中 4 本しか再現せず、チャンクを反転すると 16 本すべて再現する。
規約は参照実装のテストハーネス（`test.py` の `KAT_randbytes`）にしかない。

→ **FIPS 206 への含意**: 5 は定数を全精度で公表すべき、
   5b は ε を明記すべき、5c は KAT の規約を本文に書くべき。
   `docs/nist_comment_draft.md`。

### 6. Implementation notes（従属）

- 鍵生成 9610 → 11 ms。効いたのは全部**確保の除去**であって
  高水準の配列演算ではない。Karatsuba が schoolbook に n=2〜2048 で全敗。
- 性能は範囲比較で: `verify` 同着、`sign` は FPEMU 帯より速く native 帯に 7 倍負け、
  `keygen` は FPEMU 帯。

### 7. Timing leakage（付録）

- 脅威モデル（受理署名は公開／棄却候補は非公開）
- dudect + 対照 + 実測雑音床
- 見つけた最大の漏洩は**自分の実装の欠陥**であり参照実装には無い。
  一般化できる教訓は「漏れるのはデータ依存分岐があることではなく、
  分岐結果が一方のクラスで学習可能で他方で不能であること」。
- Julia では処理系と FPU 由来の変時間性を閉じられない、と明示。

### 8. Conclusion / Limitations

- 単一マシン。マイクロアーキテクチャ 1 種。
- 攻撃は示していない。
- FIPS 206 未発行なので、指摘は round-3 仕様書に対するもの。

---

## 投稿までにやること（優先順）

| # | 項目 | 状態 |
|--:|:---|:---|
| 1 | ~~関連研究の調査~~ | **完了**（2026-08-21）。主結果を A+B に切り替えた |
| 2 | **ePrint 2024/1709 の本文を読む** | **未。最重要。** スライドと二次引用でしか読めていない。伝播した差の正確な範囲を確認し、A(c) と重ならないことを本文で示す必要がある |
| 3 | ePrint 2024/321 の本文を読む | 未。方法論が最も近い |
| 4 | `parameters.py`（提出パッケージ）で C を裏取り | 資料待ち。`docs/deep_research_prompts.md` のプロンプト B |
| 5 | 複数マシンでの性能再測定 | 未（現環境は 1 台） |
| 6 | FALCON-1024 でも A・B を通す | 未（装置は同じ） |
| 7 | 本文執筆 | 未 |

**2 が終わるまで主結果は仮である。** 2024/1709 が既に
「代数的に同値な書き換えは伝播しない」と述べていれば、A は消える。

## 投稿先（調査結果に基づく）

| 候補 | 適性 |
|:---|:---|
| **IACR TCHES / CHES** | **第一候補。** 実装セキュリティと効率実装が主軸。ジャーナル型で改稿サイクルがあり実装詳細を厚く書ける |
| **NIST PQC Standardization Conference** | **並行して出す。** 標準化含意（B が KAT に効く条件）が直接届く。IPD のコメント期間なら pqc-forum も |
| SAC | 第三候補。"Efficient implementations" 常設トラック。分量規定は年で変わるので CFP を確認すること |
| JCEN | 分量制約が緩い。方法論を詳述するなら |
| PQCrypto / ACNS | 不適（設計・解析寄り／応用寄り） |
